## Learned User Preferences
- For UI/layout changes, the user expects iterative visual refinement from screenshots until spacing/order matches exactly.

## Learned Workspace Facts
- The home hero layout requirement is: on mobile, order content as hero text -> hero image -> Tech focus.
- The home hero portrait should keep a fixed size and should not be dynamically resized by JavaScript.
